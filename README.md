# ai-sdk-experiments

Experiments with the Vercel AI SDK.

## Usage

Install:

```bash
npm install
# or
pnpm install
```

Develop:

```bash
npm run dev
# or
pnpm dev
```

Start embeddings database (chromadb):

```bash
docker-compose up -d --build
```
